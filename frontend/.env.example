# API base URL for backend
# Use empty string for relative URLs (development with proxy)
# Or set to full URL like http://localhost:3000 for production
VITE_API_BASE=

# Node environment
VITE_NODE_ENV=development
